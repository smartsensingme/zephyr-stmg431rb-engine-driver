description: Generic H-Bridge Engine Controller configuration (Dual PWM)

compatible: "generic-engine"

include: base.yaml

properties:
  pwms:
    type: phandle-array
    required: true

  enable-gpios:
    type: phandle-array
    required: false
